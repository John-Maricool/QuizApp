<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.johnbatista.quizapp">

    <application
        android:icon="@mipmap/ic_quiz"
        android:label="MY QUIZ APP"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailedActivity"></activity>
        <activity android:name=".MainQuizActivity"></activity>
        <activity
            android:name=".Help"
            android:parentActivityName=".MainActivity"
            tools:targetApi="jelly_bean" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>